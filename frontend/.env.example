# Backend API origin (no trailing slash). Copy to .env.local for local overrides.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Optional: show in About page / builds (e.g. CI: VITE_BUILD_COMMIT=${GITHUB_SHA})
# VITE_BUILD_COMMIT=

